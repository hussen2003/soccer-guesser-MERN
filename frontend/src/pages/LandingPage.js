import Header from '../components/header/Header.js';

// right now this is purposefully empty, its just to have a page to REDIRECT to from the login page.
const LandingPage = () =>
{
    return(
    <div>
        <Header />
    </div>
    );
};

export default LandingPage;